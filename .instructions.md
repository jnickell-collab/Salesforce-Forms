# Project Instructions (short)

This repo includes two detailed guidance files that should be consulted by any assistant or automation before making changes:

- `assistant-principles.md`
- `execution-playbook.md`

Summary:

- Read relevant files before editing.
- Use `ssgMasterTheme.css` for theme tokens; do not change it unless explicitly requested.
- Keep edits focused and well-tested.

- CSS Rule: Always reference `ssgMasterTheme.css` (force-app/main/default/lwc/ssgMasterTheme/ssgMasterTheme.css) for shared CSS variables when changing styles; do NOT modify that file in PRs without prior approval.

If an assistant runs unattended, treat this file as the high-level directive and open the detailed files for specifics.
